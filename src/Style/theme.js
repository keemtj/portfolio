const light = {
  backgroundColor: '#fff',
  fontColor: '#202124',
};

const dark = {
  backgroundColor: '#202124',
  fontColor: '#fff',
};

const theme = {
  light,
  dark,
};

export default theme;
