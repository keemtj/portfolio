const light = {
  backgroundColor: '#ffffff',
  fontColor: '#202124',
};

const dark = {
  backgroundColor: '#202124',
  fontColor: '#ffffff',
};

const theme = {
  light,
  dark,
};

export default theme;
